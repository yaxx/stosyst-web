version: '3'
services:
  react-app:
    image: client
    stdin_open: true
    ports:
      - "80:80"
      - "443:443"
    networks:
      - stosyst
    volumes:
      - ./client:/usr/src/app
      - /usr/src/app/node_modules
  api-server:
    image: api
    build: ./server/api/
    stdin_open: true
    ports:
      - "8080:8080"
    networks:
      - stosyst
    volumes:
    - ./server/api:/usr/src/app
    - /usr/src/app/node_modules
networks:
  stosyst:
    driver: bridge



  